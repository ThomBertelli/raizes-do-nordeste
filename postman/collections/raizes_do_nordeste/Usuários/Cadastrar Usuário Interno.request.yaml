$kind: http-request
description: Use GERENCIA_MATRIZ ou ADMIN. GERENTE so pode criar FUNCIONARIO. CLIENTE nao pode ser criado por esta API.
url: "{{base_url}}/api/usuarios"
method: POST
headers:
  Authorization: Bearer {{token_gerencia_matriz}}
body:
  type: json
  content: |
    {
      "nome": "Funcionario Postman",
      "email": "funcionario.postman@raizes.com",
      "senha": "Funcionario@2026",
      "perfil": "FUNCIONARIO",
      "lojaId": {{loja_id}}
    }
order: 1000
