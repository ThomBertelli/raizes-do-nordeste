$kind: http-request
name: Pagar Pedido
description: Endpoint correto e DTO real de pagamento. O body aceita apenas valor.
url: "{{base_url}}/pagamentos/{{pedido_id}}"
method: POST
headers:
  Authorization: Bearer {{token_cliente}}
  Idempotency-Key: req-878
body:
  type: json
  content: |
    {
      "valor": 36
    }
order: 1100
