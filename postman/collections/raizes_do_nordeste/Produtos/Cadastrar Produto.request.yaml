$kind: http-request
url: "{{base_url}}/api/produtos"
method: POST
headers:
  Authorization: Bearer {{token_gerencia_matriz}}
body:
  type: json
  content: |
    {
      "nome": "Buchada de Bode",
      "descricao": "Prato tipico com preparo tradicional nordestino.",
      "preco": 18.00
    }
order: 1000
