$kind: http-request
description: Cadastro publico de cliente. Retorna token JWT no corpo.
url: "{{base_url}}/auth/cadastro"
method: POST
body:
  type: json
  content: |
    {
      "nome": "Cliente Postman",
      "email": "cliente.postman@raizes.com",
      "senha": "Cliente@2026",
      "consentimentoProgramaFidelidade": true
    }
order: 1000
