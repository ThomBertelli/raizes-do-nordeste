$kind: http-request
url: "{{base_url}}/api/estoques"
method: GET
headers:
  Authorization: Bearer {{token_gerencia_matriz}}
queryParams:
  - key: lojaId
    value: "{{loja_id}}"
    disabled: true
order: 1000
